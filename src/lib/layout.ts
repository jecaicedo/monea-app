/**
 * Ancho de la columna de contenido de la app (todo lo que vive detrás de
 * RutaProtegida: cabecera + páginas). Un solo valor para que la cabecera y el
 * contenido queden alineados al mismo ancho en todas las pantallas.
 *
 * No aplica a las pantallas de autenticación (LayoutAuth), que son
 * deliberadamente angostas por ser un formulario centrado, no una columna de
 * contenido.
 */
export const ANCHO_CONTENIDO_APP = 'max-w-4xl'
